{
    "name": "innodesk",
    "version": "1.1.0",
    "desctiption": "A helpdesk API to manage issues tickets reporting by our users",
    "license": "MIT",
    "scripts": {
        "start:dev": "php artisan serve",
        "db:migrate": "php artisan migrate",
        "generate:api-doc": "php artisan l5-swagger:generate",
        "release": "standard-version",
        "dev": "vite",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.8.0",
        "standard-version": "^9.5.0",
        "vite": "^4.0.0"
    }
}
